{
  "dependencies": {
    "three": "^0.161.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.1.1"
  },
  "scripts": {
    "dev": "npx vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  }
}
